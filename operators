public class operators {
    public static void main(String[]args){
        int a=10;
        int b=20;
        if(a>10 || b>10){
            System.out.println("positive");
        }
        a+=10;
        System.out.println("the modified value of a " + a);
        b-=5;
        System.out.println("the modifies value of b " +b);
          for(int i=0;i<3;i++){
              System.out.print("x ");

        }
    }
}
